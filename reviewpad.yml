workflows:
  - name: Block merge on checklist
    run:
      - if: '!$matchString("(feat|chore|fix|test|hotfix)/\\w+", $head())'
        then: $failCheckStatus("The feature branch does not follow the naming convention.")

